% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-index.R
\name{compare_indices}
\alias{compare_indices}
\title{Plot multiple CPUE indices for comarison}
\usage{
compare_indices(
  cidx,
  CPUE_set,
  alt_CPUE1 = NULL,
  series_alt1 = NULL,
  alt_CPUE2 = NULL,
  series_alt2 = NULL,
  normalise_ENSO = FALSE,
  uncert = F
)
}
\arguments{
\item{cidx}{A named list of data frames containing the primary CPUE indices.}

\item{CPUE_set}{Character vector. The names of the elements in \code{cidx} to be plotted.}

\item{alt_CPUE1}{Optional data frame. An additional index to include in the comparison.}

\item{alt_CPUE2}{Optional data frame. A second additional index to include.}

\item{series_alt2}{Character string. The legend label for \code{alt_CPUE2}.}

\item{normalise_ENSO}{Logical. If \code{TRUE}, uses min-max scaling to bound data 
between -1 and 1. If \code{FALSE}, uses geometric mean scaling relative to the 
overlap period. Defaults to \code{FALSE}.}

\item{uncert}{Logical. If \code{TRUE}, adds uncertainty bars.}

\item{component}{Character vector. The specific \code{Index} type (e.g., 'Positive', 
'Binomial', 'Combined') to extract from each set. Defaults to 'Positive'.}

\item{componentAlt1}{Character string. The \code{Index} type to extract from \code{alt_CPUE1}.}

\item{series_alt}{Character string. The legend label for \code{alt_CPUE1}.}

\item{componentAlt2}{Character string. The \code{Index} type to extract from \code{alt_CPUE2}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Standardises and visualises multiple CPUE time series on a single plot. Supports 
geometric mean normalisation over a common overlap period or a -1 to 1 
min-max scaling for ENSO-style comparisons.
}
\details{
The function identifies the "overlap period"—the years common to all included 
series—and calculates a geometric mean for each series based only on those years. 
This ensures that the relative trends are comparable even if the time series 
have different start or end years.
}
